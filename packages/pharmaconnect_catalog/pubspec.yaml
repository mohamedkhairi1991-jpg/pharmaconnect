name: pharmaconnect_catalog
description: Shared official catalog domain foundation for PharmaConnect.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=3.32.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  pharmaconnect_backend:
    path: ../pharmaconnect_backend
  pharmaconnect_identity:
    path: ../pharmaconnect_identity
  supabase_flutter: ^2.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
